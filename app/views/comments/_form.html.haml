= form_for([@post, @post.comments.new]) do |f|
	-if  not admin_user_signed_in?
		%p
			= f.label :user_name
			%br
			= f.text_field :user_name
	%p
		= f.label :message
		%br
		= f.text_area :message, :class=>"tinymce", :rows => 5, :cols => 100
	%p
		= f.submit
